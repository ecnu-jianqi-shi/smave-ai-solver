# Claim–Evidence Ledger

This ledger prevents the manuscript from upgrading a local observation into a
broader claim. All paths are relative to the repository root.

| Manuscript claim | Primary evidence | Scope retained in the paper |
| --- | --- | --- |
| Seven PDEBench-derived workloads achieve median `1.58×–135.14×` speedups, with every bootstrap 95% lower bound above one | `build/release/pdebench-repeated-timing/evidence.txt` | 30 same-input paired process runs per family on one Apple M4 host; total-runtime ratios are the inference unit, each family wins at least 29/30 runs, and only one case exceeds `100×` |
| Across all seven workloads, counterbalanced solver order yields median SMAVE-first/classical-first speedup ratios of `0.991×–1.040×`, with maximum absolute median shift `4.0%` | `build/release/pdebench-order-sensitivity/evidence.txt` | 30 matched process pairs per family (420 measured reports); 6/7 paired-bootstrap intervals contain one, Diffusion--Sorption is `1.010× [1.001, 1.045]`, and every measured speedup under both orders exceeds one |
| SuiteSparse has 31 common-success comparisons and 8 no-common-success cases | `build/release/benchmark-overall/summary.txt` | Coverage and failure accounting, not universal convergence |
| PETSc TS has 17 complete equation comparisons | `build/release/benchmark-overall/summary.txt` | Current equation cases only |
| OpenModelica executes 535,728 embedded linear calls without external numerical continuation | `build/release/benchmark-overall/summary.txt` | Seven recorded MSL models; not full Modelica compatibility |
| Linear and nonlinear verified operators execute 6,400 held-out requests at `1.053× [1.031, 1.091]` and `1.536× [1.475, 1.589]` versus classical | `build/release/phase5/performance.txt` and `build/release/nonlinear-operator/performance.txt` | Two 16-train/64-held-out families on one Apple M4 host; speedup is the paired median over 100 complete-runtime repetitions, and all candidate, correction, gate, trace, and numerical-continuation work is timed |
| Under one shared latent-candidate/32-step weighted-Jacobi/strict-gate/numerical-continuation contract, the verified operators are `1.255× [1.194, 1.336]` and `2.661× [2.532, 2.818]` faster than the corresponding shared controls | `build/release/operator-shared-baseline/evidence.txt` | Linear control accepts 6,400/6,400 and is `0.836×` classical; nonlinear control rejects 6,400/6,400 and pays full terminal continuation at `0.592×` classical; zero failures and gate mismatches; transparent common control, not an external published implementation |
| On all 750 cases from the official HINTS 1D Poisson test set, the default production Router selects `structured-tridiagonal-direct-cpu-v1` and is `16.027× [15.654, 16.563]` faster online than the official HINTS implementation; the 10,000-query setup-amortized speedup is `16.876×` | `build/release/hints-native-baseline/evidence.txt`, its `official/`, `smave/`, and `workload/` evidence, and `paper/generated/hints_native_values.tex` | Executes official revision `0c8b712f81ed08bdf27c3a215f8edb99910f5e2f`, public code, DeepONet architecture, pretrained branch/trunk/bias weights, full 750-case dataset, 400 updates, 25:1 schedule, and `ω=2/3`; both methods are checked with the same FP64 `||Ax-b||∞/max(1,||b||∞)` contract and have zero failures; setup is separate and online timing excludes official diagnostic metrics/plotting; this is one 29-unknown 1D Poisson configuration, not a claim that a SMAVE neural operator beats HINTS or that the result generalizes to other PDEs |
| Both operator validations have zero erroneous accepts over 64 independent validation scenarios and a 95% upper bound of `4.57%` | `build/release/phase5/validation.txt` and `build/release/nonlinear-operator/validation.txt` | Scenario-level finite-sample statement; 6,400 repeated timing requests are not treated as independent validation trials |
| Calibrated routing is `2.282× [2.253, 2.309]` faster than a fixed expert | `build/release/phase4/family-router-evaluation.txt` and `build/release/operator-shared-baseline/evidence.txt` | Evaluated sparse family; not a universal routing gain |
| Complete-cost cascade ordering matches the exact minimum over all 24 permutations of a four-stage synthetic contract | `build/release/cascade-ordering/evidence.txt` | Validates the fixed-cascade exchange rule and implementation only; not probability calibration or globally optimal subset selection |
| Under a 5×5-to-6×6 size/fingerprint shift, eight experts that pass the original-equation gate in both families retain gate-passing-expert cost-rank Spearman `0.905`, maximum acceptance calibration error `0.020`, and the same selected winner; the source-selected expert is `1.139×` the fastest gate-passing held-out expert by competition median | `build/release/router-shift/evidence.txt` | One sparse family shift on one Apple M4 host; exposes nonzero complete-cost regret and requires structural filtering before probability calibration |
| Under conditioning and topology shift without refitting, the source-calibrated router retains a minimum paired-speedup 95% lower bound of `1.840`, maximum structurally filtered calibration error `0.020`, and maximum selected complete-cost regret `1.116×`; the topology axis changes five expert gate statuses with zero gate mismatch or dangerous misroute | `build/release/router-shift-matrix/evidence.txt`, `tests/generate_router_shift_matrix.py`, and `benchmark/analyze_router_shift_matrix.py` | One 5×5 sparse family, 64 scenarios and 20 repetitions per axis on one Apple M4; capability filtering is necessary because the current embedding is unchanged, and no equation-family or hardware transfer is claimed |
| The production Router propagates a profiled correction budget: the same candidate falls back at budget `0` and reaches warm-start acceptance after exactly `2` Newton iterations at budget `2`, with the original-equation gate deciding the returned result | `build/release/calibrated-correction-router/evidence.txt`, `tests/calibrated_correction_router_evidence.cpp`, and `tests/verify_calibrated_correction_router.cmake` | One nonlinear scalar probe exercises `CompetitionReport` profile application and the production Runtime; this is a control-flow mechanism result, not a universal budget or speedup claim |
| The exact finite-action recurrence is proved globally optimal under order-invariant action statistics with `O(n 2^m)` worst-case scalar states and transitions; it matches an independent exhaustive oracle on one production-path profile and 256 deterministic six-action property cases, preserves one budget per expert and index order, costs `0.900×` the best single action in the production profile, and returns only after the original-equation gate accepts | Proposition `prop:exact-finite-cascade`, `build/release/joint-route-budget/evidence.txt`, `tests/joint_route_budget_evidence.cpp`, and `tests/verify_joint_route_budget.cmake` | Ratio ordering is prior art; exactness applies only when action and suffix statistics are history-independent, all reached states fit the configured cap, and at most 63 experts are represented |
| The interaction-aware finite cascade decision problem is proved NP-complete, while an exact adjacent-transition-cost recurrence uses `O(n^2 2^m)` worst-case transitions; 256 deterministic six-action/24-transition cases match exhaustive enumeration with zero gap and change 126 independent plans, and all 4,096 directed four-vertex graphs satisfy the Hamiltonian-path reduction threshold | Propositions `prop:exact-interaction-cascade` and `prop:interaction-hardness`, `build/release/joint-route-budget/evidence.txt`, `tests/joint_route_budget_evidence.cpp`, and `tests/verify_joint_route_budget.cmake` | Transition-aware sequencing and dynamic programming are prior art; the theorem covers only immediately previous-action cost effects with history-independent acceptance, not arbitrary longer-memory interactions or learned transfer |
| A prefrozen planning-only tractability study instruments the production interaction recurrence without numerical solver execution: the deployed 12-action, `k=3` shape visits 49 states/204 recursive transitions; the largest executed 32-action, `k=6` profile visits 158,209 states/1,412,192 transitions; exact preflight matches every executed recurrence and rejects a 44-action/1,227,425-state profile before any DP-state visit under the one-million-state cap | `benchmark/data-lock/joint-route-scaling-round51-prefirst-run-contract.txt`, `build/release/joint-route-scaling-round51/evidence.txt`, `build/release/joint-route-scaling-round51/scaling.tsv`, `tests/joint_route_scaling_evidence.cpp`, and `tests/verify_joint_route_scaling.cmake` | Synthetic deterministic planning counts only; no wall-time, memory, numerical-solver, workload-speedup, multi-host, or external-reproduction claim |
| A post-hoc frozen-observation interaction audit finds 32 ordered GMRES action pairs with stable isolated failures on at least two training and two calibration matrices, but zero plan-gated candidates, zero conditional-timing rows, and zero overlap with either held-out pair set; the v5/v6 held-out sets contain 8/56 ordered pairs with Jaccard `0.143` | `benchmark/data-lock/frozen-interaction-prevalence-round52-analysis-contract.txt`, `build/release/frozen-interaction-prevalence-round52/evidence.txt`, `pair-support.tsv`, `pair-classes.tsv`, and `benchmark/analyze_frozen_interaction_prevalence.py` | Diagnostic only: isolated failures do not identify conditional cost; held-out statuses were excluded from selection/calibration; no multiplier, policy, gain, population, or external-validity claim |
| A zero-execution frozen-plan attrition audit shows that all 32 supported pairs are eliminated at unguarded top-3 selection; each frozen version has five different, development-unsupported unguarded candidates, and the control-aware final plan removes all five | `benchmark/data-lock/frozen-transition-attrition-round53-analysis-contract.txt`, `build/release/frozen-transition-attrition-round53/evidence.txt`, `transition-attrition.tsv`, `candidate-transitions.tsv`, `v5-request-plans.tsv`, `v6-request-plans.tsv`, `tests/verify_frozen_transition_attrition.cmake`, and the exact C++ route cross-check | Post hoc and diagnostic only: held-out requests are excluded from every count; no solver execution, timing inference, multiplier, gain, tuning, cohort search, or population claim |
| Training profiles select correction budgets `2` and `4` for quadratic and cubic 12-variable nonlinear families; across 64 disjoint held-out scenarios, maximum selected-plan regret is `1.000×`, maximum action calibration difference is `0`, all requests succeed, and gate mismatches are `0` | `build/release/joint-route-budget-shift/evidence.txt`, `tests/joint_route_budget_shift_evidence.cpp`, `tests/verify_joint_route_budget_shift.cmake`, and `paper/generated/joint_route_budget_shift_values.tex` | Two controlled families with 32 training/32 held-out scenarios, three repetitions, two experts, and three calibrated actions; costs and pass probabilities come from production Runtime traces; not a learned policy or broad workload-transfer claim |
| A request-conditioned model fitted from production action traces predicts cost and acceptance for 12 expert--budget actions across three nonlinear families; on 192 disjoint held-out requests it reaches `1.178×` realized exhaustive-oracle cost versus `1.451×` for static-profile and fixed-action controls, with Brier `0.0076`, ECE `0.0197`, 44 distinct plans, zero DP-oracle mismatch, and zero production gate mismatch | `build/release/request-conditioned-joint-route/evidence.txt`, `build/release/request-conditioned-joint-route/action-observations.tsv`, `build/release/request-conditioned-joint-route/request-conditioned-model.txt`, `tests/request_conditioned_joint_route_evidence.cpp`, `tests/verify_request_conditioned_joint_route.cmake`, and `paper/generated/request_conditioned_joint_route_values.tex` | Three controlled 12-variable synthetic families, 3 experts × 4 budgets, 192/96/192 train/calibration/held-out requests and three repetitions; closes the static-input gap but is not a public realistic solver portfolio or universal interaction model |
| The frozen v5 control-aware replay switches zero requests and remains `4.489972×` realized-oracle regret, or `1.718371×` global fixed; recomputed fixed and family regrets match the official first-run values within `1e-12`, and no solver is re-executed | `build/release/suitesparse-control-aware-replay-v5/evidence.txt`, `decisions.tsv`, immutable v5 first-run model/observations/summary, `benchmark/replay_control_aware_anchor.py`, and the v5 selection/payload/final manifests | Counterfactual application of the current C++ anchor rule to 24 frozen v5 requests; establishes that the guard does not retrospectively repair v5, not a newly timed or tuned policy |
| The untouched v6 SuiteSparse cohort reaches `1.000030794×` realized-oracle regret versus `1.001122945×` for global fixed and `1.756464745×` for static, a `0.109%` and `43.1%` reduction; raw conditioned regret is `1.045287436×`; all 24 requests succeed with zero failure, gate mismatch, plan-order mismatch, and DP/exhaustive mismatch | `build/release/suitesparse-request-conditioned-route-final-heldout-v6-first-run/evidence.txt`, all v6 model/observation/split/production/diagnostic artifacts, the v6 selection/payload/final/pre-first-run contracts, `tests/verify_suitesparse_request_conditioned_route.cmake`, and `paper/generated/suitesparse_request_conditioned_route_values.tex` | Six/four/three train/calibration/held-out public matrices, eight requests per held-out matrix, five repetitions per compatible action, one Apple M4 host, and exactly one prefrozen performance run; no family specialization, family adaptation, or interaction transition is selected, so this is near-oracle control selection rather than broad request adaptation |
| Across linear and nonlinear held-out operators, correction plus runtime gate accounts for `97.24%` and `99.84%` of layered full-path time, while raw candidate generation accounts for `1.18%` and `0.17%` | `build/release/complete-cost-decomposition/evidence.txt` | Two family-specific layered measurements normalized by full runtime; component timers overlap and are not asserted to be independently additive |
| The minimum full-acceptance correction budgets are `0` for the linear family and `2` for the nonlinear family; nonlinear budget `1` accepts `1.56%`, while budget `2` costs `0.655×` budget `0` after candidate, correction, gate, trace, and numerical continuation are included | `build/release/complete-cost-decomposition/evidence.txt`, generated macros in `paper/generated/solver_analysis_values.tex`, and `tests/verify_complete_cost_decomposition.cmake` | Budgets `0,1,2,4,8,16,32`, 30 rotated repetitions and 1,920 requests per point; two evaluated families on one Apple M4, not a universal optimal-budget claim |
| Fused strict gates are `1.772×` and `1.337×` faster across 100 repetitions of 65 requests per workload | `build/release/gate-architecture/evidence.txt` | Gate-kernel speedup with observed decision/residual equivalence |
| Ten-worker Navier--Stokes gate scaling reaches `2.068× [1.982, 2.172]`, while the complete verified path reaches `1.207× [1.100, 1.354]` | `build/release/parallel-scaling/evidence.txt` | 30 paired intra-node repetitions, explicit worker counts, Accelerate internal threads fixed to one; one Apple M4 host only |
| Ten-worker fused-gate scaling reaches `3.514× [3.295, 3.905]` on a linear family and `2.403× [2.171, 2.458]` on a nonlinear family | `build/release/gate-parallel-scaling/evidence.txt` | 30 paired repetitions of 2,080 requests per family; gate-only scaling with zero decision and residual mismatches |
| At ten requested workers, increasing the Navier--Stokes batch from 12 to 160 real-data solves yields `2.432× [2.256, 2.521]` complete-throughput scaling | `build/release/batch-scaling/evidence.txt` | 30 paired repetitions, alternating batch order, normalized to the 12-solve case; intra-node amortization rather than strong scaling |
| Metal and ANE candidates execute and pass generic device/reference checks | `build/release/device-execution/evidence.txt` | Functional heterogeneous execution; PDEBench-specific device paths can be slower or rejected |
| Installed C/C++ SDK and frozen ABI hosts pass | `build/release/c-api-sdk/installed-evidence.txt` and `build/release/c-api-abi-matrix/` | Current build and frozen v1 compatibility evidence |
| Clean Ubuntu 24.04 ARM64 and emulated x86-64 builds each pass all 29 CTest tests | `build/portability/linux-arm64/evidence.txt` and `build/portability/linux-amd64/evidence.txt` | Build correctness and API portability only; not native Linux performance or independent reproduction |
| A deterministic core archive is generated twice byte-identically and an author-operated clean extraction rebuilds Release, passes 29/29 CTests, reruns the exhaustive cascade-ordering and paper evidence checks, and rebuilds the PDF | `build/core-repro-bundle/{contract.txt,clean-tree-evidence.txt,smave-core-repro.tar.gz.sha256}` and `artifact/verify_core_repro_bundle.py` | Core source/evidence reproduction only; 47-GB PDEBench and 61 non-fixture locked SuiteSparse systems are excluded; no public archive, persistent identifier, independent operator, or external performance claim |
| License-aware byte locks verify 79 consumed benchmark files: seven PDEBench DaRUS v8.0 files (50,937,093,313 bytes) and 66 SuiteSparse matrices plus six RHS files (3,911,822,320 bytes) | `benchmark/data-lock/{pdebench.tsv,suitesparse.tsv,verify_data_lock.py}` and `build/release/data-lock/evidence.txt` | Sizes, official PDEBench MD5, SHA-256, embedded matrix metadata, CC BY 4.0 licenses, and live upstream records are checked; payloads are not redistributed in the core archive, upstream hosts are mutable, and no public mirror or independent rerun is claimed |
| Every successfully returned accelerated result must pass its family-specific original-equation gate | `build/release/component-ablation/evidence.txt` and solver validation traces | A solver control-flow invariant and tested implementation path; not formal verification of an incorrectly specified callback, floating-point hardware, or arbitrary external code |

## Claims deliberately excluded

- Universal or cross-platform `100×` acceleration.
- Zero future erroneous accepts.
- Full Modelica, Simulink, FMI, SSP, arbitrary high-index DAE, or general hybrid semantics.
